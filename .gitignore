public/
.hugo_build.lock
.DS_Store
*.code-workspace

# Blog engine
node_modules/
.env

# Syndication cache (token exchange results)
.cache/

# Ebook working copies (source files — do not commit)
TempEbookfolder/
private/

# Email subscriber log — local only, never committed
static/_data/email-subscribers.json
